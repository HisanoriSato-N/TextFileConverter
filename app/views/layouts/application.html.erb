<!DOCTYPE html>
<html>
  <head>
    <title>Stem</title>
    <%= favicon_link_tag '/update_icon.ico' %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

  </head>
  <% if flash[:alert] %>
    <div class = "flash text-center p-3 mb-2 bg-danger text-white">
      <%= flash[:alert] %>
    </div>
  <% end %> 
  <body>
    <%= yield %>
  </body>
</html>
